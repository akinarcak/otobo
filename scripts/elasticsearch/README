This directory serves as the Docker build context for careoncloud.eleasticsearch.dockerfile.
The directory is left empty deliberately, because no files from CareOnCloud ESM are needed for building that image.
